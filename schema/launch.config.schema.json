{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "LaunchConfig": {
      "type": "object",
      "required": [
        "profiles"
      ],
      "properties": {
        "profiles": {
          "type": "object",
          "required": [],
          "properties": {},
          "additionalProperties": {
            "$ref": "#/definitions/BuildProfile"
          },
          "description": "Build profiles keyed by name."
        },
        "credentials": {
          "type": "string",
          "description": "Registered name of the credentials provider to use. Defaults to `local` (serves both platforms)."
        },
        "storage": {
          "type": "string",
          "description": "Registered name of the artifact storage provider to use. Defaults to `local`."
        },
        "buildEngine": {
          "type": "string",
          "description": "Registered name of the build engine. Carries the iOS default `fastlane` (or `eas` for the cloud handoff); an Android build swaps that iOS baseline for its twin `gradle` unless overridden here."
        },
        "submit": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "$ref": "#/definitions/SubmitByPlatform"
            }
          ],
          "description": "Where built artifacts are submitted, in one of two forms: a single registered submitter name (the iOS default `app-store-connect`, which an Android build swaps for its twin `google-play`; or `eas`) — the original, unchanged shape; or a per-platform {@link SubmitByPlatform} map, to fan one build out to several stores from this one config (e.g. an Android `.aab` to `google-play` and `amazon-appstore`). The pipeline resolves this to a store list per platform (see `resolveSubmitters`), so the build target and the store are no longer welded 1:1. See `docs/adr/0006-platform-store-split.md`."
        },
        "appRoots": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Glob roots to scan for apps. Defaults to the repo root."
        },
        "products": {
          "type": "object",
          "required": [],
          "properties": {},
          "additionalProperties": {
            "$ref": "#/definitions/AppProducts"
          },
          "description": "Declarative App Store Connect product catalog, keyed by iOS bundle id. Drives `launch sync`, which reconciles each app's subscriptions, in-app purchases, and pricing on App Store Connect to match this. Absent for apps that sell nothing. See {@link AppProducts}."
        },
        "notify": {
          "type": "object",
          "required": [],
          "properties": {
            "webhookUrl": {
              "type": "string",
              "description": "Incoming-webhook URL posted a JSON body on each transition. The payload carries both `text` (Slack) and `content` (Discord) set to a human summary, plus the structured event fields, so a Slack or Discord webhook renders it directly and a custom endpoint can read the typed data."
            },
            "command": {
              "type": "string",
              "description": "Shell command run on each transition with the event in its environment as `LAUNCH_*` vars (`LAUNCH_EVENT`, `LAUNCH_STATUS`, `LAUNCH_APP`, `LAUNCH_VERSION`; plus `LAUNCH_BUILD_NUMBER`, `LAUNCH_DESTINATION`, `LAUNCH_ERROR` on build/submit, or `LAUNCH_DETAIL` on review/rollout). Runs under `/bin/sh -c`, like a git hook."
            },
            "events": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "build",
                  "submit",
                  "review",
                  "rollout"
                ]
              },
              "description": "Which transitions fire a notification. Absent = all."
            }
          },
          "additionalProperties": false,
          "description": "Build/submit completion notifications (webhook + shell hook). Absent = no notifications. See {@link NotifyConfig}."
        },
        "release": {
          "type": "object",
          "required": [],
          "properties": {
            "releaseType": {
              "type": "string",
              "enum": [
                "AFTER_APPROVAL",
                "MANUAL",
                "SCHEDULED"
              ],
              "description": "How an approved build reaches the store. Defaults to `AFTER_APPROVAL`. Overridable with `--manual`/`--scheduled`."
            },
            "earliestReleaseDate": {
              "type": "string",
              "description": "ISO-8601 instant to go live at — only meaningful with `releaseType: \"SCHEDULED\"` (ignored otherwise). A `--scheduled <iso>` flag sets both this and the release type for one run."
            },
            "phasedRelease": {
              "type": "boolean",
              "description": "Opt into Apple's 7-day phased release (a gradual percentage rollout) for an approved update. Defaults to `false` — an immediate 100% release. Overridable per-run with `--phased`, and steerable afterward with `launch rollout <pause|resume|complete>`. Ignored for a first version (Apple only phases updates)."
            },
            "usesNonExemptEncryption": {
              "type": "boolean",
              "description": "Whether the binary contains non-exempt encryption (Apple's export-compliance question). `false` — the common case for apps using only standard HTTPS/system crypto — lets Launch declare compliance over the API so the build clears `WAITING_FOR_EXPORT_COMPLIANCE` without a portal trip. Set `true` only if you ship proprietary/non-exempt encryption; Launch then stops and points you to the portal, since genuine non-exempt encryption requires documentation Apple's API can't accept. Defaults to `false`."
            },
            "releaseNotes": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "object",
                  "required": [],
                  "properties": {},
                  "additionalProperties": {
                    "type": "string"
                  }
                }
              ],
              "description": "Release notes (\"What's New in This Version\"), per App Store locale (e.g. `{ \"en-US\": \"Bug fixes.\" }`) or a single string applied to {@link ReleaseConfig.primaryLocale}. When absent, Launch reuses the previous version's notes so a release never ships an empty \"What's New\". Apple stores these on the version's localization, not the version itself."
            },
            "primaryLocale": {
              "type": "string",
              "description": "Primary App Store locale for a bare-string {@link ReleaseConfig.releaseNotes}. Defaults to `en-US`."
            }
          },
          "additionalProperties": false,
          "description": "iOS public-release policy for `launch release` (release type, scheduled date, phased rollout, export compliance, release notes). Absent = the safe defaults (go live after approval, all at once). See {@link ReleaseConfig}."
        },
        "gameCenter": {
          "type": "object",
          "required": [],
          "properties": {},
          "additionalProperties": {
            "$ref": "#/definitions/GameCenterConfig"
          },
          "description": "Game Center achievements & leaderboards, keyed by iOS bundle id. Drives `launch game-center`. The single-config form of `gamecenter.config.json` (still accepted for back-compat). See {@link GameCenterConfig}."
        },
        "appClips": {
          "type": "object",
          "required": [],
          "properties": {},
          "additionalProperties": {
            "$ref": "#/definitions/AppClipsConfig"
          },
          "description": "App Clip card metadata, keyed by the parent app's iOS bundle id. Drives `launch app-clips`. The single-config form of `appclips.config.json` (still accepted for back-compat). See {@link AppClipsConfig}."
        },
        "releaseAttributes": {
          "type": "object",
          "required": [],
          "properties": {},
          "additionalProperties": {
            "$ref": "#/definitions/ReleaseAttributesConfig"
          },
          "description": "App Store release attributes (age rating, categories, price, review details), keyed by iOS bundle id. Drives `launch release-config`. The single-config form of `release.config.json` (still accepted for back-compat). Distinct from {@link LaunchConfig.release} (the release policy). See {@link ReleaseAttributesConfig}."
        },
        "wallet": {
          "type": "object",
          "required": [],
          "properties": {
            "merchantIds": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/WalletIdConfig"
              },
              "description": "Apple Pay merchant ids to register."
            },
            "passTypeIds": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/WalletIdConfig"
              },
              "description": "Wallet pass type ids to register."
            }
          },
          "additionalProperties": false,
          "description": "Team-level Apple Pay merchant ids & Wallet pass type ids. Drives `launch wallet`. The single-config form of `wallet.config.json` (still accepted for back-compat). See {@link WalletConfig}."
        },
        "euDistribution": {
          "type": "object",
          "required": [
            "domains"
          ],
          "properties": {
            "domains": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/EuDistributionDomainConfig"
              },
              "description": "Domains to authorize for EU alternative distribution."
            }
          },
          "additionalProperties": false,
          "description": "Team-level EU alternative-distribution domains (DMA). Drives `launch eu-distribution`. The single-config form of `eu-distribution.config.json` (still accepted for back-compat). See {@link EuDistributionConfig}."
        },
        "configFiles": {
          "type": "object",
          "required": [],
          "properties": {
            "availability": {
              "type": "string"
            },
            "accessibility": {
              "type": "string"
            },
            "experiments": {
              "type": "string"
            },
            "customPages": {
              "type": "string"
            }
          },
          "additionalProperties": false,
          "description": "Optional non-default paths for the sidecar-only surfaces' `*.config.json` files (availability, accessibility, experiments, custom pages). Lets `launch plan` / `launch drift` find a sidecar that isn't at its default filename, since those surfaces have no typed field here. Omit to use defaults. See {@link SurfaceConfigFiles}."
        },
        "aws": {
          "type": "object",
          "required": [
            "region"
          ],
          "properties": {
            "region": {
              "type": "string",
              "description": "AWS region to allocate the Dedicated Host in (e.g. `us-east-1`)."
            },
            "profile": {
              "type": "string",
              "description": "Named profile in `~/.aws` to resolve via the credential chain. Omit to use the default chain."
            },
            "amiId": {
              "type": "string",
              "description": "BYO golden AMI id. Omit to bootstrap + snapshot one into your own account on first use."
            },
            "instanceType": {
              "type": "string",
              "description": "EC2 Mac instance type. Defaults to `mac2.metal` (cheapest M-series in most regions)."
            }
          },
          "additionalProperties": false,
          "description": "AWS EC2 Mac settings for remote (off-Mac) builds. Only needed when building via `--remote aws`."
        },
        "storageConfig": {
          "type": "object",
          "required": [
            "bucket",
            "publicBaseUrl"
          ],
          "properties": {
            "endpoint": {
              "type": "string",
              "description": "S3-compatible endpoint, e.g. `https://<account>.r2.cloudflarestorage.com` (Cloudflare R2), a Backblaze B2 / MinIO endpoint, etc. Omit for AWS S3 (the SDK derives it from the region). Unused by the `supabase` provider."
            },
            "bucket": {
              "type": "string",
              "description": "Bucket name (S3-compatible) or storage bucket id (Supabase)."
            },
            "region": {
              "type": "string",
              "description": "Region for an S3-compatible provider. Defaults to `auto` (correct for R2) when omitted; unused by Supabase."
            },
            "publicBaseUrl": {
              "type": "string",
              "description": "Public base URL that maps to the bucket root — used to build install links and OTA manifest URLs. e.g. an R2 custom domain `https://cdn.example.com`, or a Supabase public object URL prefix `https://<project>.supabase.co/storage/v1/object/public/<bucket>`. No trailing slash required."
            },
            "supabaseUrl": {
              "type": "string",
              "description": "Supabase project URL (`https://<project>.supabase.co`). Required by `supabase`, unused by `s3`."
            }
          },
          "additionalProperties": false,
          "description": "Bucket/endpoint settings for a cloud {@link StorageProvider} (`s3` / `supabase`). Required when `storage` names a cloud provider — it's where ad-hoc install links and OTA update manifests are hosted. Secrets stay out: access keys resolve from env / the OS secret store, never from here."
        },
        "artifactDir": {
          "type": "string",
          "description": "Where the `local` storage provider writes build binaries and raw objects (install plists, OTA manifests). A relative path resolves against the project root (the `launch.config.ts` directory); a leading `~/` expands to the home directory; an absolute path is used as-is. Omit to use the global `~/.launch/artifacts` (the default — existing projects are unaffected). `launch init` and the no-args wizard scaffold this as the in-repo `./.launch/artifacts` and add it to `.gitignore`, so build binaries never get committed. Only the `local` provider observes it — cloud stores key off {@link StorageConfig}. The history index stays under `~/.launch`, so build history and retention span projects regardless of where the binaries land."
        },
        "artifactRetentionDays": {
          "type": "number",
          "description": "How many days a local build binary is kept before the artifact store auto-prunes it to reclaim disk (the newest build per app+platform is always kept, so a promotable artifact never disappears). Runs after each successful local build. Defaults to 30 when omitted; set to `0` to disable the automatic sweep entirely (`launch builds prune` still works on demand). Only the `local` provider observes this — cloud stores manage retention through their own bucket lifecycle rules."
        },
        "envExclude": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Env var names that must NEVER be injected into a build — a hard denylist applied across every layer (`.env`, `.env.<profile>`, keychain, profile `env:`, even an explicit `--env`). A matched name is dropped outright, so it can't reach the build subprocess and therefore can't be baked into the shipped app even by an `app.config.js` that forwards `process.env`.\n\nEach entry is either an exact, case-sensitive name or a `PREFIX*` wildcard: `OPENAI_*` drops every name starting with `OPENAI_` (e.g. `OPENAI_API_KEY`, `OPENAI_ORG_ID`), so a whole family of backend keys collapses to one line instead of being listed individually. Wildcards anchor at the START — there is no tail/`*_KEY` form, by design, since that would also snag a publishable `EXPO_PUBLIC_..._KEY`.\n\nThis is the home for *backend-only* values that sit in the app's `.env` for local tooling but must never ship (e.g. `OPENAI_API_KEY`, a server-side `SENTRY_AUTH_TOKEN`). It is distinct from `launch secret set`: a stored secret is still *injected* — the build needs it — it's just moved out of plaintext; `envExclude` means \"don't inject this at all\". A name matched here is exempt from the `.env.example` missing-key gate (even when no layer sets it). Omit (or `[]`) to exclude nothing."
        },
        "mcp": {
          "type": "object",
          "required": [],
          "properties": {
            "capabilities": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": [
                  "read",
                  "dryRun",
                  "write",
                  "dangerous"
                ]
              },
              "description": "Which capability tiers the MCP server may expose. Each enabled tier unlocks the tools tagged at that tier; omit (or `[]`) for `[\"read\"]` — read-only. Listing a higher tier does not imply the lower ones, so `[\"read\", \"write\"]` is the usual \"let agents read everything and run reconciles\" posture."
            }
          },
          "additionalProperties": false,
          "description": "How `launch mcp` exposes Launch to AI agents — chiefly which capability tiers it may offer. Absent = least privilege (read-only tools). See {@link McpConfig}."
        }
      },
      "additionalProperties": false,
      "description": "The fully-resolved configuration for one `launch` invocation. Produced by {@link loadConfig} from `launch.config.ts` plus auto-discovered apps. Names here (`storage`, `credentials`, `buildEngine`) are looked up in the provider registry at runtime."
    },
    "BuildProfile": {
      "type": "object",
      "required": [
        "name"
      ],
      "properties": {
        "name": {
          "type": "string",
          "description": "Profile name as referenced by `--profile`."
        },
        "envFile": {
          "type": "string",
          "description": "Dotenv file to load for this profile, relative to the app dir. Defaults to `.env`."
        },
        "env": {
          "type": "object",
          "required": [],
          "properties": {},
          "additionalProperties": {
            "type": "string"
          },
          "description": "Inline env vars for this profile, merged into the build/update/release environment. They sit above the dotenv files (`.env.local`, `.env.<profile>`, `.env`) but below keychain secrets and `--env` flags in the precedence ladder — see `core/env.ts` `resolveEnv`. Use for non-secret, committed config that should travel with the profile; keep real secrets in `launch secret`."
        },
        "ssl": {
          "type": "boolean",
          "description": "Enable SSL pinning for this profile (mirrors the existing build.ts toggle). Defaults to false."
        },
        "sizeBudgetMB": {
          "type": "number",
          "description": "Per-device download-size budget in megabytes. When the size report exceeds it, the build soft-gates (asks for confirmation) rather than failing. Defaults to 200 (Apple's cellular line)."
        },
        "track": {
          "type": "string",
          "enum": [
            "internal",
            "closed",
            "open",
            "production"
          ],
          "description": "Android-only: default Play track for `launch build android` when `--track` is omitted. Defaults to `internal` (the only safe target for a fresh account). Ignored on iOS."
        },
        "rollout": {
          "type": "number",
          "description": "Android-only: default staged-rollout fraction (0–1) for production releases when `--rollout` is omitted. Defaults to `1.0` (full rollout). Ignored on iOS."
        }
      },
      "additionalProperties": false,
      "description": "A named build profile from `launch.config.ts` (e.g. `production`, `preview`). Holds only Launch-specific settings; app facts stay in `app.json`. A profile maps to a `.env` file whose values are injected into the build and gates the artifact on size."
    },
    "SubmitByPlatform": {
      "type": "object",
      "required": [],
      "properties": {
        "ios": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "android": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "tvos": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "macos": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "visionos": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "AppProducts": {
      "type": "object",
      "required": [],
      "properties": {
        "subscriptionGroups": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/SubscriptionGroupConfig"
          },
          "description": "Auto-renewable subscription groups and the subscriptions within them."
        },
        "inAppPurchases": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/InAppPurchaseConfig"
          },
          "description": "One-off in-app purchases."
        },
        "promotedPurchases": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/PromotedPurchaseConfig"
          },
          "description": "Promoted purchases in product-page display order (`launch offers` reorders the live list to match)."
        }
      },
      "additionalProperties": false,
      "description": "The declarative App Store Connect product catalog for ONE app, keyed by iOS bundle id under {@link LaunchConfig.products}. `launch sync` reconciles the live account to match this: it creates missing groups/subscriptions/IAPs, fills in localizations, and sets prices. `launch offers` reconciles the subscription offers nested under {@link SubscriptionGroupConfig} and the {@link AppProducts.promotedPurchases} ordering. All fields are optional so an app can sell only subscriptions, only one-off purchases, or (with none set) nothing."
    },
    "SubscriptionGroupConfig": {
      "type": "object",
      "required": [
        "referenceName",
        "localizations",
        "subscriptions"
      ],
      "properties": {
        "referenceName": {
          "type": "string",
          "description": "Internal reference name (unique within the app) — the reconciler's natural key for the group."
        },
        "localizations": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/GroupLocalization"
          },
          "description": "Per-locale group display name; at least one entry is required (else the group's subs stay unsubmittable)."
        },
        "subscriptions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/SubscriptionConfig"
          },
          "description": "The subscription levels in this group."
        }
      },
      "additionalProperties": false,
      "description": "A subscription group — Apple's container for mutually-exclusive subscription levels (a customer holds at most one active subscription per group). `referenceName` is unique within the app and is the reconciler's natural key for the group."
    },
    "GroupLocalization": {
      "type": "object",
      "required": [
        "locale",
        "name"
      ],
      "properties": {
        "locale": {
          "type": "string",
          "description": "App Store locale code, e.g. `en-US`."
        },
        "name": {
          "type": "string",
          "description": "Customer-facing group name."
        }
      },
      "additionalProperties": false,
      "description": "One locale's display name for a subscription GROUP. Groups carry only a name (no description); it's shown at the point of purchase grouping the subscription levels. Without one, every subscription in the group is stuck in \"Missing Metadata\", so at least one is required per group."
    },
    "SubscriptionConfig": {
      "type": "object",
      "required": [
        "productId",
        "referenceName",
        "subscriptionPeriod",
        "localizations"
      ],
      "properties": {
        "productId": {
          "type": "string",
          "description": "Apple product id, e.g. `com.acme.pro.monthly`. Globally unique; the reconciler matches on it."
        },
        "referenceName": {
          "type": "string",
          "description": "Internal reference name shown only in App Store Connect (Apple limit: 64 characters)."
        },
        "subscriptionPeriod": {
          "type": "string",
          "enum": [
            "ONE_WEEK",
            "ONE_MONTH",
            "TWO_MONTHS",
            "THREE_MONTHS",
            "SIX_MONTHS",
            "ONE_YEAR"
          ],
          "description": "Billing period for this level."
        },
        "localizations": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ProductLocalization"
          },
          "description": "Per-locale display copy; at least one entry is required for a submittable product."
        },
        "price": {
          "type": "object",
          "required": [
            "customerPrice"
          ],
          "properties": {
            "baseTerritory": {
              "type": "string",
              "description": "Base territory whose price point is matched, e.g. `USA`. Defaults to `USA`."
            },
            "customerPrice": {
              "type": "number",
              "description": "Exact customer-facing price in the base territory's currency, e.g. `9.99`. Must equal an Apple price point."
            }
          },
          "additionalProperties": false,
          "description": "Baseline price. Omit only to price manually in the UI."
        },
        "offerCodes": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/OfferCodeConfig"
          },
          "description": "Offer-code campaigns to ensure exist on this subscription (`launch offers`)."
        },
        "promotionalOffers": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/PromotionalOfferConfig"
          },
          "description": "Promotional offers to ensure exist on this subscription."
        },
        "introductoryOffers": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/IntroductoryOfferConfig"
          },
          "description": "Introductory offers (at most one per territory) to ensure exist on this subscription."
        },
        "winBackOffers": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/WinBackOfferConfig"
          },
          "description": "Win-back offers to ensure exist on this subscription."
        },
        "reviewScreenshot": {
          "type": "string",
          "description": "Path (relative to the app directory) to this subscription's App Review screenshot — the image Apple requires before a subscription can be submitted. `launch sync` uploads it via the reservation flow, idempotently: it's skipped when the live screenshot's MD5 already matches the local file. Omit to attach it by hand in App Store Connect. Reconciled in `core/ascScreenshots.ts`, not here."
        },
        "play": {
          "type": "object",
          "required": [
            "prices"
          ],
          "properties": {
            "productId": {
              "type": "string",
              "description": "Play subscription product id; defaults to the shared {@link SubscriptionConfig.productId}."
            },
            "basePlanId": {
              "type": "string",
              "description": "Base-plan id; defaults to a slug of the billing period (e.g. `p1m`)."
            },
            "prices": {
              "type": "object",
              "required": [],
              "properties": {},
              "additionalProperties": {
                "$ref": "#/definitions/PlayPriceConfig"
              },
              "description": "Per-region base-plan prices (region code → micro-units + currency). At least one region required."
            },
            "offers": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/PlaySubscriptionOfferConfig"
              },
              "description": "Offers (free trials / introductory pricing) to ensure exist on the base plan."
            }
          },
          "additionalProperties": false,
          "description": "Google Play overrides; present this to also publish the subscription to Play (see {@link PlaySubscriptionOverride})."
        }
      },
      "additionalProperties": false,
      "description": "One auto-renewable subscription product inside a {@link SubscriptionGroupConfig}. `productId` is the globally-unique Apple product id the app references at runtime and the reconciler's natural key. Add a {@link PlaySubscriptionOverride} under `play` to also publish it to Google Play."
    },
    "ProductLocalization": {
      "type": "object",
      "required": [
        "locale",
        "name"
      ],
      "properties": {
        "locale": {
          "type": "string",
          "description": "App Store locale code, e.g. `en-US`."
        },
        "name": {
          "type": "string",
          "description": "Customer-facing display name (Apple limit: 30 characters)."
        },
        "description": {
          "type": "string",
          "description": "Customer-facing description (Apple limit: 45 characters). Omitted when not provided."
        }
      },
      "additionalProperties": false,
      "description": "One locale's customer-facing copy for a subscription or in-app purchase — the display name (and optional description) shown on the product page. Apple keeps a product in \"Missing Metadata\" until it has at least one localization, so the reconciler rejects an empty list rather than silently creating an unsubmittable product. The `locale` is the natural key the reconciler matches on."
    },
    "OfferCodeConfig": {
      "type": "object",
      "required": [
        "duration",
        "offerMode",
        "numberOfPeriods",
        "name",
        "customerEligibilities",
        "offerEligibility"
      ],
      "properties": {
        "duration": {
          "type": "string",
          "enum": [
            "THREE_DAYS",
            "ONE_WEEK",
            "TWO_WEEKS",
            "ONE_MONTH",
            "TWO_MONTHS",
            "THREE_MONTHS",
            "SIX_MONTHS",
            "ONE_YEAR"
          ],
          "description": "Offer billing duration unit."
        },
        "offerMode": {
          "type": "string",
          "enum": [
            "PAY_AS_YOU_GO",
            "PAY_UP_FRONT",
            "FREE_TRIAL"
          ],
          "description": "How the offer discounts. `FREE_TRIAL` must omit {@link OfferConfigBase.prices}."
        },
        "numberOfPeriods": {
          "type": "number",
          "description": "How many {@link OfferConfigBase.duration} units the offer spans."
        },
        "prices": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/OfferPrice"
          },
          "description": "Per-territory discounted prices. Required unless `offerMode` is `FREE_TRIAL`."
        },
        "name": {
          "type": "string",
          "description": "Campaign name shown in App Store Connect — unique per subscription; the reconciler's key."
        },
        "customerEligibilities": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "NEW",
              "EXISTING",
              "EXPIRED"
            ]
          },
          "description": "Which customers may redeem the code."
        },
        "offerEligibility": {
          "type": "string",
          "enum": [
            "STACK_WITH_INTRO_OFFERS",
            "REPLACE_INTRO_OFFERS"
          ],
          "description": "Whether the code stacks with or replaces the intro offer."
        }
      },
      "additionalProperties": false,
      "description": "A subscription offer-code campaign (Apple's `subscriptionOfferCodes`) — a redeemable promo that grants an introductory price. `name` is the reconciler's natural key (unique per subscription); offer-code terms are immutable once created, so the reconciler only ever creates a missing code, never edits one (deactivation is the explicit `launch offers deactivate` action). One-time-use and custom code batches are generated separately (the imperative `launch offers codes` subcommands), not declared here."
    },
    "OfferPrice": {
      "type": "object",
      "required": [
        "customerPrice"
      ],
      "properties": {
        "territory": {
          "type": "string",
          "description": "Territory whose price point is matched, e.g. `USA`. Defaults to `USA`."
        },
        "customerPrice": {
          "type": "number",
          "description": "Exact customer-facing price in the territory's currency, e.g. `4.99`. Must equal an Apple price point."
        }
      },
      "additionalProperties": false,
      "description": "One territory's discounted price for an offer, resolved to an Apple subscription price point exactly like {@link ProductPrice} (the customer-facing amount must equal a point on Apple's fixed ladder). Omit prices entirely for a `FREE_TRIAL` offer."
    },
    "PromotionalOfferConfig": {
      "type": "object",
      "required": [
        "duration",
        "offerMode",
        "numberOfPeriods",
        "name",
        "offerCode"
      ],
      "properties": {
        "duration": {
          "type": "string",
          "enum": [
            "THREE_DAYS",
            "ONE_WEEK",
            "TWO_WEEKS",
            "ONE_MONTH",
            "TWO_MONTHS",
            "THREE_MONTHS",
            "SIX_MONTHS",
            "ONE_YEAR"
          ],
          "description": "Offer billing duration unit."
        },
        "offerMode": {
          "type": "string",
          "enum": [
            "PAY_AS_YOU_GO",
            "PAY_UP_FRONT",
            "FREE_TRIAL"
          ],
          "description": "How the offer discounts. `FREE_TRIAL` must omit {@link OfferConfigBase.prices}."
        },
        "numberOfPeriods": {
          "type": "number",
          "description": "How many {@link OfferConfigBase.duration} units the offer spans."
        },
        "prices": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/OfferPrice"
          },
          "description": "Per-territory discounted prices. Required unless `offerMode` is `FREE_TRIAL`."
        },
        "name": {
          "type": "string",
          "description": "Internal name shown in App Store Connect."
        },
        "offerCode": {
          "type": "string",
          "description": "Product-level offer identifier the app references in StoreKit — the reconciler's key."
        }
      },
      "additionalProperties": false,
      "description": "A promotional offer (Apple's `subscriptionPromotionalOffers`) — a developer-presented discount surfaced in-app to existing/lapsed subscribers. `offerCode` is the product-level identifier the app passes to StoreKit at redemption; it is the reconciler's natural key (unique per subscription)."
    },
    "IntroductoryOfferConfig": {
      "type": "object",
      "required": [
        "duration",
        "offerMode",
        "numberOfPeriods"
      ],
      "properties": {
        "duration": {
          "type": "string",
          "enum": [
            "THREE_DAYS",
            "ONE_WEEK",
            "TWO_WEEKS",
            "ONE_MONTH",
            "TWO_MONTHS",
            "THREE_MONTHS",
            "SIX_MONTHS",
            "ONE_YEAR"
          ],
          "description": "Billing duration unit."
        },
        "offerMode": {
          "type": "string",
          "enum": [
            "PAY_AS_YOU_GO",
            "PAY_UP_FRONT",
            "FREE_TRIAL"
          ],
          "description": "How the offer discounts. `FREE_TRIAL` must omit {@link IntroductoryOfferConfig.price}."
        },
        "numberOfPeriods": {
          "type": "number",
          "description": "How many `duration` units the offer spans."
        },
        "territory": {
          "type": "string",
          "description": "Territory this intro offer applies to (the reconciler's key); omit for all territories."
        },
        "price": {
          "type": "object",
          "required": [
            "customerPrice"
          ],
          "properties": {
            "territory": {
              "type": "string",
              "description": "Territory whose price point is matched, e.g. `USA`. Defaults to `USA`."
            },
            "customerPrice": {
              "type": "number",
              "description": "Exact customer-facing price in the territory's currency, e.g. `4.99`. Must equal an Apple price point."
            }
          },
          "additionalProperties": false,
          "description": "The discounted price in {@link IntroductoryOfferConfig.territory}. Required unless `FREE_TRIAL`."
        },
        "startDate": {
          "type": "string",
          "description": "ISO date (`YYYY-MM-DD`) the offer starts; omit to start immediately."
        },
        "endDate": {
          "type": "string",
          "description": "ISO date (`YYYY-MM-DD`) the offer ends; omit for no end."
        }
      },
      "additionalProperties": false,
      "description": "An introductory offer (Apple's `subscriptionIntroductoryOffers`) — the one auto-applied first-time discount. Apple allows at most one per (subscription, territory); when `territory` is omitted it applies to all territories the subscription is sold in. `territory` is the reconciler's natural key."
    },
    "WinBackOfferConfig": {
      "type": "object",
      "required": [
        "duration",
        "offerMode",
        "numberOfPeriods",
        "offerId",
        "referenceName",
        "eligiblePaidMonths",
        "monthsSinceLastSubscribed",
        "startDate"
      ],
      "properties": {
        "duration": {
          "type": "string",
          "enum": [
            "THREE_DAYS",
            "ONE_WEEK",
            "TWO_WEEKS",
            "ONE_MONTH",
            "TWO_MONTHS",
            "THREE_MONTHS",
            "SIX_MONTHS",
            "ONE_YEAR"
          ],
          "description": "Offer billing duration unit."
        },
        "offerMode": {
          "type": "string",
          "enum": [
            "PAY_AS_YOU_GO",
            "PAY_UP_FRONT",
            "FREE_TRIAL"
          ],
          "description": "How the offer discounts. `FREE_TRIAL` must omit {@link OfferConfigBase.prices}."
        },
        "numberOfPeriods": {
          "type": "number",
          "description": "How many {@link OfferConfigBase.duration} units the offer spans."
        },
        "prices": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/OfferPrice"
          },
          "description": "Per-territory discounted prices. Required unless `offerMode` is `FREE_TRIAL`."
        },
        "offerId": {
          "type": "string",
          "description": "Stable offer identifier the app references — the reconciler's key (unique within the app)."
        },
        "referenceName": {
          "type": "string",
          "description": "Internal reference name shown in App Store Connect."
        },
        "eligiblePaidMonths": {
          "type": "number",
          "description": "Minimum months the customer must previously have paid to be eligible."
        },
        "monthsSinceLastSubscribed": {
          "type": "object",
          "required": [
            "min",
            "max"
          ],
          "properties": {
            "min": {
              "type": "number"
            },
            "max": {
              "type": "number"
            }
          },
          "additionalProperties": false,
          "description": "Eligible window since the customer last subscribed, in months (inclusive `[min, max]`)."
        },
        "waitBetweenOffersMonths": {
          "type": "number",
          "description": "Minimum months to wait between showing successive win-back offers; omit for Apple's default."
        },
        "startDate": {
          "type": "string",
          "description": "ISO date (`YYYY-MM-DD`) the offer starts. Required by Apple."
        },
        "endDate": {
          "type": "string",
          "description": "ISO date (`YYYY-MM-DD`) the offer ends; omit for no end."
        },
        "priority": {
          "type": "string",
          "enum": [
            "HIGH",
            "NORMAL"
          ],
          "description": "Display priority among competing win-back offers. Defaults to `NORMAL`."
        },
        "promotionIntent": {
          "type": "string",
          "enum": [
            "NOT_PROMOTED",
            "USE_AUTO_GENERATED_ASSETS"
          ],
          "description": "Whether Apple auto-generates promotion artwork (`USE_AUTO_GENERATED_ASSETS`) or the offer isn't promoted."
        }
      },
      "additionalProperties": false,
      "description": "A win-back offer (Apple's `winBackOffers`) — a discount shown on the App Store to lapsed subscribers, gated on how long they previously paid and how long ago they churned. `offerId` is the reconciler's natural key (unique within the app). Win-back offers carry no images here — promotion artwork is the `promotionIntent` auto-generated path; custom artwork is a deferred follow-up."
    },
    "PlayPriceConfig": {
      "type": "object",
      "required": [
        "priceMicros",
        "currency"
      ],
      "properties": {
        "priceMicros": {
          "type": "string",
          "description": "Amount in micro-units: 1,000,000 = one whole unit of `currency`."
        },
        "currency": {
          "type": "string",
          "description": "ISO 4217 currency code, e.g. `USD`."
        }
      },
      "additionalProperties": false,
      "description": "A Google Play price: an exact amount in a currency's micro-units (millionths) plus the ISO currency code. Play has no price-point ladder — `\"1990000\"` with currency `\"USD\"` is $1.99. Used for both a product's default price and any per-region overrides. Kept distinct from {@link ProductPrice} because the two stores model money differently (Apple resolves a fixed price point; Play takes a literal micro-unit amount), so a single shared price field can't serve both."
    },
    "PlaySubscriptionOfferConfig": {
      "type": "object",
      "required": [
        "offerId"
      ],
      "properties": {
        "offerId": {
          "type": "string",
          "description": "Play offer id (unique within the base plan)."
        },
        "freeTrialDuration": {
          "type": "string",
          "description": "Free-trial length as an ISO-8601 duration (e.g. `P1W`, `P1M`). Omit for no trial phase."
        },
        "introPrices": {
          "type": "object",
          "required": [],
          "properties": {},
          "additionalProperties": {
            "$ref": "#/definitions/PlayPriceConfig"
          },
          "description": "Introductory per-region prices (region code → micro-units + currency). Omit for no intro phase."
        },
        "introRecurrenceCount": {
          "type": "number",
          "description": "How many billing periods the introductory price repeats for. Defaults to 1."
        }
      },
      "additionalProperties": false,
      "description": "One Google Play offer on a subscription's base plan — a free trial, an introductory price, or both (Play allows up to two offer phases). `offerId` is the natural key the reconciler matches on. Set `freeTrialDuration` for a free phase and/or `introPrices` for a discounted phase; an offer with neither is rejected (it would discount nothing)."
    },
    "InAppPurchaseConfig": {
      "type": "object",
      "required": [
        "productId",
        "referenceName",
        "type",
        "localizations"
      ],
      "properties": {
        "productId": {
          "type": "string",
          "description": "Apple product id, e.g. `com.acme.coins.100`. Globally unique; the reconciler matches on it."
        },
        "referenceName": {
          "type": "string",
          "description": "Internal reference name shown only in App Store Connect."
        },
        "type": {
          "type": "string",
          "enum": [
            "CONSUMABLE",
            "NON_CONSUMABLE",
            "NON_RENEWING_SUBSCRIPTION"
          ],
          "description": "The purchase kind."
        },
        "localizations": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ProductLocalization"
          },
          "description": "Per-locale display copy; at least one entry is required for a submittable product."
        },
        "price": {
          "type": "object",
          "required": [
            "customerPrice"
          ],
          "properties": {
            "baseTerritory": {
              "type": "string",
              "description": "Base territory whose price point is matched, e.g. `USA`. Defaults to `USA`."
            },
            "customerPrice": {
              "type": "number",
              "description": "Exact customer-facing price in the base territory's currency, e.g. `9.99`. Must equal an Apple price point."
            }
          },
          "additionalProperties": false,
          "description": "Baseline price. Omit only to price manually in the UI."
        },
        "play": {
          "type": "object",
          "required": [],
          "properties": {
            "sku": {
              "type": "string",
              "description": "Play SKU; defaults to the shared {@link InAppPurchaseConfig.productId} when omitted."
            },
            "defaultPrice": {
              "type": "object",
              "required": [
                "priceMicros",
                "currency"
              ],
              "properties": {
                "priceMicros": {
                  "type": "string",
                  "description": "Amount in micro-units: 1,000,000 = one whole unit of `currency`."
                },
                "currency": {
                  "type": "string",
                  "description": "ISO 4217 currency code, e.g. `USD`."
                }
              },
              "additionalProperties": false,
              "description": "Default price applied to every region without an explicit {@link PlayProductOverride.prices} entry."
            },
            "prices": {
              "type": "object",
              "required": [],
              "properties": {},
              "additionalProperties": {
                "$ref": "#/definitions/PlayPriceConfig"
              },
              "description": "Per-region price overrides keyed by ISO region code (e.g. `US`)."
            }
          },
          "additionalProperties": false,
          "description": "Google Play overrides; present this to also publish the product to Play (see {@link PlayProductOverride})."
        }
      },
      "additionalProperties": false,
      "description": "One non-subscription in-app purchase (consumable, non-consumable, or non-renewing subscription). `productId` is the globally-unique Apple product id and the reconciler's natural key. Add a {@link PlayProductOverride} under `play` to also publish it to Google Play."
    },
    "PromotedPurchaseConfig": {
      "type": "object",
      "required": [
        "productId"
      ],
      "properties": {
        "productId": {
          "type": "string",
          "description": "Apple product id of the subscription or in-app purchase to promote."
        },
        "visibleForAllUsers": {
          "type": "boolean",
          "description": "Whether the promotion is visible to all users (vs. targeted via the API). Defaults to `true`."
        },
        "enabled": {
          "type": "boolean",
          "description": "Whether the promotion is enabled. Defaults to `true`."
        }
      },
      "additionalProperties": false,
      "description": "One promoted purchase (Apple's `promotedPurchases`) — an IAP or subscription surfaced on the app's App Store product page. Declaration order in {@link AppProducts.promotedPurchases} is the display order Apple shows; `launch offers` reorders the live list to match. `productId` references an existing subscription or in-app purchase; the reconciler resolves it to the live resource."
    },
    "GameCenterConfig": {
      "type": "object",
      "required": [],
      "properties": {
        "achievements": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/AchievementConfig"
          }
        },
        "leaderboards": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/LeaderboardConfig"
          }
        }
      },
      "additionalProperties": false,
      "description": "An app's declared Game Center achievements and leaderboards — the `gamecenter.config.json` document, or one entry of {@link LaunchConfig.gameCenter} (keyed by iOS bundle id). Either list may be omitted. Reconciled additively by `launch game-center`."
    },
    "AchievementConfig": {
      "type": "object",
      "required": [
        "vendorIdentifier",
        "referenceName",
        "points",
        "name",
        "beforeEarnedDescription",
        "afterEarnedDescription"
      ],
      "properties": {
        "vendorIdentifier": {
          "type": "string",
          "description": "Developer-chosen stable id used to match config to Apple's record (never shown to players)."
        },
        "referenceName": {
          "type": "string",
          "description": "Internal name shown in App Store Connect."
        },
        "points": {
          "type": "number",
          "description": "Points awarded (Apple caps the total across achievements at 1000)."
        },
        "showBeforeEarned": {
          "type": "boolean",
          "description": "Whether the achievement is visible to players before it's earned (default false)."
        },
        "repeatable": {
          "type": "boolean",
          "description": "Whether it can be earned more than once (default false)."
        },
        "name": {
          "type": "string",
          "description": "Player-facing title in the localization."
        },
        "beforeEarnedDescription": {
          "type": "string",
          "description": "Player-facing description shown before the achievement is earned."
        },
        "afterEarnedDescription": {
          "type": "string",
          "description": "Player-facing description shown after it's earned."
        },
        "locale": {
          "type": "string",
          "description": "Locale for the localization above (default `en-US`)."
        }
      },
      "additionalProperties": false,
      "description": "One declared Game Center achievement: Apple's create attributes plus its default-locale localization."
    },
    "LeaderboardConfig": {
      "type": "object",
      "required": [
        "vendorIdentifier",
        "referenceName",
        "defaultFormatter",
        "submissionType",
        "scoreSortType",
        "name"
      ],
      "properties": {
        "vendorIdentifier": {
          "type": "string"
        },
        "referenceName": {
          "type": "string"
        },
        "defaultFormatter": {
          "type": "string",
          "enum": [
            "INTEGER",
            "DECIMAL_POINT_1_PLACE",
            "DECIMAL_POINT_2_PLACE",
            "DECIMAL_POINT_3_PLACE",
            "ELAPSED_TIME_CENTISECOND",
            "ELAPSED_TIME_MINUTE",
            "ELAPSED_TIME_SECOND",
            "MONEY_POUND_DECIMAL",
            "MONEY_POUND",
            "MONEY_DOLLAR_DECIMAL",
            "MONEY_DOLLAR",
            "MONEY_EURO_DECIMAL",
            "MONEY_EURO",
            "MONEY_FRANC_DECIMAL",
            "MONEY_FRANC",
            "MONEY_KRONER_DECIMAL",
            "MONEY_KRONER",
            "MONEY_YEN"
          ],
          "description": "How scores are formatted (e.g. `INTEGER`, `ELAPSED_TIME_SECOND`)."
        },
        "submissionType": {
          "type": "string",
          "enum": [
            "BEST_SCORE",
            "MOST_RECENT_SCORE"
          ],
          "description": "Whether the board keeps each player's best or most recent score."
        },
        "scoreSortType": {
          "type": "string",
          "enum": [
            "ASC",
            "DESC"
          ],
          "description": "Whether higher (`DESC`) or lower (`ASC`) scores rank first."
        },
        "name": {
          "type": "string",
          "description": "Player-facing title in the localization."
        },
        "locale": {
          "type": "string",
          "description": "Locale for the localization above (default `en-US`)."
        }
      },
      "additionalProperties": false,
      "description": "One declared Game Center leaderboard: Apple's create attributes plus its default-locale localization name."
    },
    "AppClipsConfig": {
      "type": "object",
      "required": [
        "clips"
      ],
      "properties": {
        "clips": {
          "type": "object",
          "required": [],
          "properties": {},
          "additionalProperties": {
            "$ref": "#/definitions/AppClipConfig"
          }
        }
      },
      "additionalProperties": false,
      "description": "An app's declared App Clips — the `appclips.config.json` document, or one entry of {@link LaunchConfig.appClips} (keyed by the parent app's iOS bundle id). Each App Clip is keyed by its own bundle id (e.g. `com.acme.app.Clip`), which is how a config entry is matched to the clip the build produced. Reconciled by `launch app-clips`."
    },
    "AppClipConfig": {
      "type": "object",
      "required": [],
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "OPEN",
            "VIEW",
            "PLAY"
          ],
          "description": "The card's call-to-action button (`OPEN` / `VIEW` / `PLAY`)."
        },
        "localizations": {
          "type": "object",
          "required": [],
          "properties": {},
          "additionalProperties": {
            "$ref": "#/definitions/AppClipLocalizationConfig"
          },
          "description": "Per-locale card subtitles, keyed by Apple locale (e.g. `en-US`)."
        }
      },
      "additionalProperties": false,
      "description": "One App Clip's declared card metadata. Both fields are optional and reconciled independently, so a clip may declare just an `action`, just `localizations`, or both."
    },
    "AppClipLocalizationConfig": {
      "type": "object",
      "required": [
        "subtitle"
      ],
      "properties": {
        "subtitle": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "description": "One locale of an App Clip card: the subtitle shown under the app name in that locale."
    },
    "ReleaseAttributesConfig": {
      "type": "object",
      "required": [],
      "properties": {
        "ageRating": {
          "type": "object",
          "required": [],
          "properties": {},
          "additionalProperties": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "boolean"
              }
            ]
          },
          "description": "Age-rating answers as Apple's `name → value` map (enum strings or booleans); only changed keys are sent."
        },
        "categories": {
          "$ref": "#/definitions/ReleaseCategories"
        },
        "pricing": {
          "$ref": "#/definitions/ReleasePricing"
        },
        "reviewDetails": {
          "$ref": "#/definitions/ReviewDetailsConfig"
        }
      },
      "additionalProperties": false,
      "description": "An app's declared App Store *release attributes* — age rating, App Store categories, base price, and App Review details — the `release.config.json` document, or one entry of {@link LaunchConfig.releaseAttributes} (keyed by iOS bundle id). Every section is optional and reconciled independently by `launch release-config`, so a file may declare only the attribute(s) you manage as code (e.g. just `pricing`). Named to avoid colliding with {@link ReleaseConfig}, which is the distinct iOS *release policy* (when/how a version goes live)."
    },
    "ReleaseCategories": {
      "type": "object",
      "required": [],
      "properties": {
        "primary": {
          "type": "string"
        },
        "secondary": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "description": "Declared primary/secondary App Store categories (`appCategories` ids such as `PRODUCTIVITY`)."
    },
    "ReleasePricing": {
      "type": "object",
      "required": [
        "customerPrice"
      ],
      "properties": {
        "baseTerritory": {
          "type": "string",
          "description": "Base territory to anchor the price on (default `USA`)."
        },
        "customerPrice": {
          "type": "number",
          "description": "The customer-facing price in the base territory; must match one of Apple's price-ladder rungs."
        }
      },
      "additionalProperties": false,
      "description": "Declared base price: a customer price (e.g. `9.99`) in a base territory Apple equalizes from."
    },
    "ReviewDetailsConfig": {
      "type": "object",
      "required": [],
      "properties": {
        "contactFirstName": {
          "type": "string"
        },
        "contactLastName": {
          "type": "string"
        },
        "contactPhone": {
          "type": "string"
        },
        "contactEmail": {
          "type": "string"
        },
        "demoAccountRequired": {
          "type": "boolean"
        },
        "demoAccountName": {
          "type": "string"
        },
        "demoAccountPassword": {
          "type": "string",
          "description": "The reviewer demo-account password. Prefer an indirection over a plaintext literal so the secret needn't sit in a repo-committed config (per \"secrets never touch the repo\"): `env:VAR_NAME` reads it from the environment, `keychain:ACCOUNT` from the OS keychain — both resolved only at submit time, so a plan never reads or holds it. Any other value is used as a literal (backward compatible)."
        },
        "notes": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "description": "Declared App Review details: the contact Apple reaches and the demo account its reviewer signs in with. Field names match Apple's `appStoreReviewDetails` attributes verbatim. `demoAccountPassword` is never read back from Apple or logged."
    },
    "WalletIdConfig": {
      "type": "object",
      "required": [
        "identifier",
        "name"
      ],
      "properties": {
        "identifier": {
          "type": "string",
          "description": "The identifier to register (e.g. `merchant.com.acme.app` or `pass.com.acme.coupon`)."
        },
        "name": {
          "type": "string",
          "description": "A label shown in App Store Connect / the developer portal."
        }
      },
      "additionalProperties": false,
      "description": "One declared Apple identifier: the reverse-DNS id plus a human-readable name shown in the portal."
    },
    "EuDistributionDomainConfig": {
      "type": "object",
      "required": [
        "domain",
        "referenceName"
      ],
      "properties": {
        "domain": {
          "type": "string",
          "description": "The domain authorized to host distribution packages (e.g. `downloads.acme.com`)."
        },
        "referenceName": {
          "type": "string",
          "description": "A label shown in App Store Connect to identify the domain."
        }
      },
      "additionalProperties": false,
      "description": "One authorized EU distribution domain: the host plus a human-readable reference name."
    }
  },
  "$ref": "#/definitions/LaunchConfig"
}
