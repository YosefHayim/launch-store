# Dependencies & build output
node_modules/
dist/
*.tsbuildinfo

# Secrets — NEVER commit. Launch keeps these in the macOS Keychain / ~/.launch.
.env
.env.*.local
*.p8
*.p12
*.mobileprovision
*.keystore
*.jks
AuthKey_*.p8

# Launch's local state (artifacts, metadata cache)
.launch/

# Generated native projects (recreated by `expo prebuild`)
ios/
android/

# OS / editor noise
.DS_Store
*.log
