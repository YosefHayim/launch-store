# Committed template of the env keys this app expects. Copy to .env and fill real values.
# This sample declares none, so `launch build ios --dry-run` runs with no setup.
#
# NOTE: there is no EXPO_PUBLIC_ prefix guard — anything in .env can reach the app bundle.
# Keep backend secrets OUT of this file; Launch warns on secret-looking names.
