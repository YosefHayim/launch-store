`$left ?? $right` as $fallback where {
  register_diagnostic(span=$fallback, message="Decode absence at the owning boundary instead of using ?? (CODE-STYLE.md: control.explicit).", severity="error")
}
