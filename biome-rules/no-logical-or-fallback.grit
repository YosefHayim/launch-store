`$left || $right` as $fallback where {
  register_diagnostic(span=$fallback, message="Use an explicit domain branch instead of logical-or fallback (CODE-STYLE.md: control.explicit).", severity="error")
}
